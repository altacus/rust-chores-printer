[package]
name = "tasks"
version = "0.1.0"
edition = "2024"

[dependencies]
recibo = { version = "0.3.0", features = ["graphics"] }
image = "0.24" # Required for image processing
gtk4 = "0.8"
chrono = "0.4"